import Tickable from "src/interfaces/tickable.interface";

/**
 * This class handles all of the logic for an enemy.
 */
export default class Tower implements Tickable {

    /**
     * The enemy constructor.
     */
    constructor() {

    }

    /**
     * Tick the enemy.
     */
    tick(): void {
    }
}